import { FC, useState } from "react";

const Browse: FC = () => {
  const [songs, setSongs] = useState([]);

  const handleClick = () => {
    console.log("clicked");
  };

  return <></>;
};

export default Browse;
